# Evolutionary Game Theory

Making a evolutionary game theory "system" to start playing with

# Setup

## Windows

Have administrative privileges and run from cmd

```sh
python -m venv --clear --prompt evogt venv
venv\Scripts\activate.bat
pip install -e .[dev]
```

## Bash

```sh
python3 -m venv --clear --prompt evogt venv
. venv/bin/activate
pip install -e .[dev]
```

# Formatter

```sh
format # just type this
```

# Linters

```sh
verify # just type this
```

# Unit Tests

```sh
pytest
```
